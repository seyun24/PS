#include <bits/stdc++.h>
using namespace std;
int t;
int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int a, b,c;
    cin >> a>> b>>c;
    t = a * 60 + b + c;
    if (t >= 1440) {
        t-=1440;
    }
    cout<<t / 60<<' '<< t % 60;
    
  

}
