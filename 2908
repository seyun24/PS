#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    string a, b,c;
    cin >> a >> b;
   
    if (a[2] > b[2])
        c = a;
     else 
        c = b;

    cout << c[2]<<c[1]<<c[0];
  
    
 }
